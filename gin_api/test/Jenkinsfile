pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'golang:1.22-alpine'
                    args '-u root:root -e GOCACHE=/tmp/.cache'
                }
            }
            steps {
                // 1. Pull the API code into its own folder
                dir('api-repo') {
                    git url: 'https://github.com/Pailin-h3/jenkins.git', branch: 'develop'
                    
                    sh 'apk add --no-cache git' 
                    sh 'go mod download'
                    // 2. Build and move the binary UP to the root workspace
                    sh 'go build -buildvcs=false -o ../api-binary .'
                }
            }
        }
        
        stage('Deploy') {
            agent any
            steps {
                script {
                    sh 'docker rm -f gin-api-test || true'
                    sh 'rm -f Dockerfile'
                    
                    writeFile file: 'Dockerfile', text: """
                        FROM alpine:latest
                        RUN apk --no-cache add ca-certificates
                        WORKDIR /root/
                        COPY api-binary .
                        RUN chmod +x api-binary
                        # Force Gin to release mode to skip the 'localhost' debug message
                        ENV GIN_MODE=release
                        CMD ["./api-binary"]
                    """
                    
                    sh 'docker build --no-cache -t gin-api-test:latest .'
                    sh 'docker run -d --name gin-api-test -p 3000:8080 gin-api-test:latest'
                    
                    echo "API is live at http://localhost:3000"
                }
            }
        }
    }
}